import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Join",
  description: "Join a group or community on Krishna Connect.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
