import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Get Verified",
  description: "Apply for a verified badge on Krishna Connect.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
