export const metadata = {
  title: 'FAQ',
  description: 'Frequently asked questions about Krishna Connect'
};

export default function FAQLayout({ children }: { children: React.ReactNode }) {
  return children;
}
